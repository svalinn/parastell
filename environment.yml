name: parastell_env
channels:
  - conda-forge
dependencies:
  - python=3.11.6
  - pip=24.0
  - numpy
  - scipy
  - scikit-learn
  - cadquery
  - moab==5.5.0[build=nompi_tempest_*]
  - cad_to_dagmc
  - ca-certificates
  - certifi
  - openssl
  - matplotlib
  - pip:
      - netcdf4
      - pyyaml
      - pytest